FROM nginx:latest

# Copy the vulnerable web application files to the appropriate directory
COPY webapp /usr/share/nginx/html

# Expose the HTTP port
EXPOSE 80

# Start the nginx web server
CMD ["nginx", "-g", "daemon off;"]

